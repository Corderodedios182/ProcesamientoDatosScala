procesamientoJob {
  params {
    devName = ${DEV_NAME}
    currentYear = ${YEAR}
  }

  input {

    fdevCustomersParquet = {
      type = parquet
      paths = ["procesamiento/src/test/resources/data.input/parquet/t_fdev_customers"]
    }

  }

}
